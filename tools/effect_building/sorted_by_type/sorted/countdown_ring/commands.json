{
  "commands": {
    "overlay_countdown_ring_assist": {
      "kind": "state_toggle",
      "off": {
        "action": "clear_layer",
        "target_layer": "TEMP_OVERLAY_LAYER"
      },
      "on": {
        "preset": "overlay_countdown_ring_assist"
      }
    },
    "overlay_countdown_ring_guide": {
      "kind": "state_toggle",
      "off": {
        "action": "clear_layer",
        "target_layer": "TEMP_OVERLAY_LAYER"
      },
      "on": {
        "preset": "overlay_countdown_ring_guide"
      }
    },
    "overlay_countdown_ring_marker": {
      "kind": "state_toggle",
      "off": {
        "action": "clear_layer",
        "target_layer": "TEMP_OVERLAY_LAYER"
      },
      "on": {
        "preset": "overlay_countdown_ring_marker"
      }
    },
    "overlay_countdown_ring_tracking": {
      "kind": "state_toggle",
      "off": {
        "action": "clear_layer",
        "target_layer": "TEMP_OVERLAY_LAYER"
      },
      "on": {
        "preset": "overlay_countdown_ring_tracking"
      }
    }
  }
}