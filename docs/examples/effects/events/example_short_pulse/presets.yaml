# Optional embedded presets for this effect.
# Presets configure this definition and cannot change its type or lifecycle.
presets:
  example_short_pulse_event_example:
    title: "Example Short Pulse Event Example"
    description: "Kurzer blauer Bestaetigungsimpuls fuer das Tutorial."
    params:
      color: "#33AAFF"
      brightness: 1.0
      duration_ms: 600
      speed: 1.0
    tags:
      - tutorial
