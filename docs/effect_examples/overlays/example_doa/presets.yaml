# Optional embedded presets for this effect.
# Presets configure this definition and cannot change its type or lifecycle.
presets:
  example_doa_overlay_example:
    title: "Example DoA Overlay Example"
    description: "Gruene Richtungsmarkierung fuer das DoA-Tutorial."
    params:
      color: "#33FFAA"
      brightness: 1.0
      width: 1
    tags:
      - tutorial
