# Optional embedded presets for this effect.
# Presets configure this definition and cannot change its type or lifecycle.
presets:
  template_overlay_pull_example:
    title: "Template Pull Overlay Example"
    description: "TODO: describe this preset."
    params:
      color: "#33AAFF"
    tags:
      - example
